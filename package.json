{
  "name": "vivox-token",
  "version": "0.0.2",
  "description": "create vivox token",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "license": "MIT",
  "keywords": [
    "vivox-token",
    "Vivox"
  ],
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "jest",
    "build": "yarn format; tsc",
    "format": "prettier --write 'src/**/*.ts' ; prettier --write 'test/**/*.ts' ",
    "prepublishOnly": "yarn build"
  },
  "devDependencies": {
    "@types/jest": "^25.1.2",
    "@types/node": "^12.12.20",
    "jest": "^25.1.0",
    "prettier": "2.0.4",
    "ts-jest": "^25.2.0",
    "tslint": "^6.1.1",
    "typescript": "^3.7.3"
  },
  "jest": {
    "moduleFileExtensions": [
      "js",
      "ts"
    ],
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "testMatch": [
      "./**/*.ts"
    ],
    "rootDir": "test"
  },
  "prettier": {
    "singleQuote": true,
    "semi": true,
    "tabWidth": 2,
    "bracketSpacing": true
  }
}
